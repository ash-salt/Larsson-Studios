using UnityEngine;
/*
public class SweepAttack : MonoBehaviour, Action
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    int damage;

    public int getCost()
    {
        return 1;
    }

    public void act(MapTile tile1, MapTile tile2, MapTile tile3)
    {
        MapTile[] tiles = {tile1, tile2, tile3};
        foreach (MapTile tile in tiles)
        {
            GameObject target = tile.getContainedObject();
            if (target is Enemy)
            {
                var enemy = target as Enemy;
                enemy.takeDamage(damage);
            }   
        }
    }
}
*/